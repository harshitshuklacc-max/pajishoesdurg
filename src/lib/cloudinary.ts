/**
 * @deprecated Import from `@/lib/cloudinary-url` (client) or `@/lib/cloudinary.server` (API routes).
 * Kept for server-side imports that only need URL helpers.
 */
export {
  cloudinaryUrl,
  faviconFromLogo,
  validateUpload,
  type ImagePreset,
} from "./cloudinary-url";
